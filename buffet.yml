# This is the configuration file we use to test Buffet itself.
# See buffet.sample.yml for a more extended example with
# annotations.
slaves:
  - user: buffet
    host: sds
  - user: buffet
    host: humus
  - user: buffet
    host: non-existant-hostname-for-testing

project:
  name: buffet
  directory: ~/buffet

allowed_slave_prepare_failures: 1
